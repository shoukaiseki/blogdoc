# ubuntu精品软件  

```
#rm 删除后放到回收站  安装完在 /etc/profile 增加 alias rm=“trash-put”
apt-get install trash-cli

#任务栏显示网速
sudo add-apt-repository ppa:fossfreedom/indicator-sysmonitor
sudo apt-get update
sudo apt-get install indicator-sysmonitor
```
